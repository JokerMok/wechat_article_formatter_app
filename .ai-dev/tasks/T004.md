# T004 图文测量、分页和绘制引擎

- 需求：REQ-008、REQ-013
- 测试：TEST-013、TEST-014、TEST-015、TEST-023、TEST-024
- 依赖：T001
- 批次：Wave 2
- 建议执行者：cloud
- 风险：high
- 允许改动：`lib/renderers/cards/`
- 禁止改动：旧页面组件、平台文案规则、存储、导出、包依赖和 `.ai-dev/`；不得通过裁切或隐藏溢出来通过测试。
- 快照排除目录：`node_modules/`、`.next/`、`coverage/`
- 输入：现有 Canvas 测量、分页、绘制和贴图逻辑；`3:4`、`9:16` 安全区域；字号和间距重排规则。
- 输出：可注入测量器、页面布局树、流式分页、锁定页处理、Canvas 绘制、溢出检测和单元测试。
- 验收：文本顺序完整；参数变化重新测量全部受影响页面；标题避免孤页；极端输入无死循环和截断。
- 验证：`npx vitest run lib/renderers/cards`；`npx tsc --noEmit`；`npm run lint -- lib/renderers/cards`
- 回滚：丢弃 T004 worktree；旧 Canvas 逻辑在新工作区切换前继续工作。
