# T006 AI 生成、校验和降级

- 需求：REQ-004、REQ-013、REQ-014
- 测试：TEST-007、TEST-008、TEST-009、TEST-021、TEST-022
- 依赖：T001
- 批次：Wave 2
- 建议执行者：cloud
- 风险：high
- 允许改动：`lib/ai/`、`tests/fixtures/ai/`
- 禁止改动：工作区、存储实现、平台渲染、导出、包依赖和 `.ai-dev/`；不得记录密钥、正文或模型完整响应。
- 快照排除目录：`node_modules/`、`.next/`、`coverage/`
- 输入：OpenAI-compatible 接口、T001 内容结构、平台输出字段、超时/取消/限流和固定结构要求。
- 输出：模型提供方接口、请求取消与超时、Zod 校验、差异记录、错误分类、确定性降级和固定响应夹具。
- 验收：合法响应可用；非法响应不覆盖数据；超时、429 和取消有明确结果；默认不持久化密钥；无 AI 时可降级。
- 验证：`npx vitest run lib/ai`；`npx tsc --noEmit`；`npm run lint -- lib/ai tests/fixtures/ai`
- 回滚：丢弃 T006 worktree；平台基础版本不依赖 AI 模块运行。
