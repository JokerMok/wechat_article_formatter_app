# T008 统一工作区与平台编辑

- 需求：REQ-001、REQ-003、REQ-005、REQ-006、REQ-007、REQ-008、REQ-009、REQ-010、REQ-012、REQ-013
- 测试：TEST-001、TEST-005、TEST-006、TEST-010 至 TEST-016、TEST-020、TEST-021、TEST-023
- 依赖：T002、T003、T004、T005、T006
- 批次：Wave 3
- 建议执行者：cloud
- 推荐模型：GPT-5.3-Codex-Spark；由完整 Codex 负责架构边界复核和独立审查
- 风险：high
- 允许改动：`app/page.tsx`、`app/layout.tsx`、`app/globals.css`、`components/workspace/`、`components/ui/dialog.tsx`、`components/ui/slider.tsx`、`components/ui/toggle.tsx`、`components/ui/tooltip.tsx`
- 禁止改动：旧单体组件、核心模块内部实现、包依赖、导出模块和 `.ai-dev/`；不得在组件中复制平台规则或分页算法。
- 快照排除目录：`node_modules/`、`.next/`、`coverage/`
- 输入：T002 至 T006 公共接口、统一工作区布局、平台编辑隔离、图片操作和窄屏视图要求。
- 输出：项目栏、源文与素材区、平台版本编辑区、真实尺寸预览、排版设置、平台切换、错误状态和组件测试。
- 验收：桌面三栏和窄屏视图均可用；平台切换不丢修改；字号和间距调整触发重排；图片操作和锁定、撤销、重做可执行。
- 验证：`npx vitest run components/workspace`；`npx tsc --noEmit`；`npm run lint -- app components/workspace components/ui/dialog.tsx components/ui/slider.tsx components/ui/toggle.tsx components/ui/tooltip.tsx`；`npm run build`
- 回滚：恢复 `app/page.tsx` 对旧组件的引用；不删除新模块和本地数据。
