# T001 统一内容结构与依赖基线

- 需求：REQ-002、REQ-014
- 测试：TEST-003、TEST-004、TEST-022、TEST-024
- 依赖：无
- 批次：Wave 1
- 建议执行者：cloud
- 风险：high
- 允许改动：`package.json`、`package-lock.json`、`lib/article-types.ts`、`lib/article-parser.ts`、`lib/article-parser.test.ts`、`lib/content/`、`lib/platforms/types.ts`
- 禁止改动：页面组件、公众号样式、Canvas 绘制、存储、导出和 `.ai-dev/`；不得删除现有解析测试或降低断言。
- 快照排除目录：`node_modules/`、`.next/`、`coverage/`
- 输入：现有 `ArticleBlock`、解析规则、PRD REQ-002/REQ-014、异常文本夹具要求。
- 输出：统一内容结构、平台版本公共类型、来源位置、Markdown/普通文本标准化、兼容转换函数和单元测试；一次性锁定 `unified`、`remark-parse`、`remark-gfm`、`zod`、`zustand`、`dexie`、`jszip`、Testing Library、fake IndexedDB、jsdom 和 Playwright 依赖。
- 验收：有效正文不静默丢失；空引用和残留样式被过滤；异常输入降级；开发和端到端测试依赖可从锁文件重复安装；现有 14 项基线测试保持通过。
- 验证：`npx vitest run lib/article-parser.test.ts lib/content`；`npx tsc --noEmit`；`npm run lint -- lib/article-types.ts lib/article-parser.ts lib/content lib/platforms/types.ts`
- 回滚：丢弃 T001 worktree；未合并前不影响现有入口。
