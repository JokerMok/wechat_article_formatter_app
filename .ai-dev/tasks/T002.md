# T002 本地项目与素材存储

- 需求：REQ-001、REQ-009、REQ-010、REQ-013
- 测试：TEST-001、TEST-002、TEST-016、TEST-017、TEST-021
- 依赖：T001
- 批次：Wave 2
- 建议执行者：cloud
- 风险：medium
- 允许改动：`lib/assets/`、`lib/storage/`、`store/`
- 禁止改动：页面、平台生成、渲染、导出、包依赖和 `.ai-dev/`；不得静默删除无法迁移的数据。
- 快照排除目录：`node_modules/`、`.next/`、`coverage/`
- 输入：T001 内容项目结构、IndexedDB 约束、800 毫秒自动保存要求和存储错误分类。
- 输出：图片校验与裁剪参数、项目仓库、素材 Blob 仓库、Zustand 状态、自动保存、数据版本迁移、备份读取接口和测试。
- 验收：项目和图片刷新可恢复；写入失败不误报成功；未知版本保留原始数据；无项目时返回明确空状态。
- 验证：`npx vitest run lib/assets lib/storage store`；`npx tsc --noEmit`；`npm run lint -- lib/assets lib/storage store`
- 回滚：丢弃 T002 worktree；新数据库使用独立名称，不修改旧浏览器记录。
